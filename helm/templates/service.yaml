apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName | default .Values.appName | default "auth-service" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.appName | default "auth-service" }}
spec:
  type: {{ .Values.service.type }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP | default "192.168.1.200" }}  # Fixed EXTERNAL-IP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
  selector:
    app: {{ .Values.appName | default "auth-service" }}
    color: {{ .Values.color }}
