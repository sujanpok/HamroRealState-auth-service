apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName | default .Values.appName | default "auth-service" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.appName | default "auth-service" }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
  selector:
    app: {{ .Values.appName | default "auth-service" }}
    color: {{ .Values.color }}
